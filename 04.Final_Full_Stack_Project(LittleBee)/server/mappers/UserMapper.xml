<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">
    <!-- 로그인 아이디, 비밀번호 검색 -->
    <select id="findIdPw">
        SELECT usernum, username, userid, useraddress, userphone, useremail, gender, birthdate, secession, signuptime FROM member WHERE userid=#{userid} AND userpw=#{userpw}
    </select>

    <!-- 단일행 조회를 위한 기능 정의 -->
    <select id="selectItem">
        SELECT usernum, username, userid, userphone, useraddress, useremail FROM member WHERE usernum=#{usernum}
    </select>

    <!-- ID 찾기 -->
    <select id="findId">
        SELECT userid, username FROM member WHERE username=#{username} AND useremail=#{useremail}
    </select>

    <!-- 다중행 조회를 위한 기능 정의 -->
    <select id="selectList">
        SELECT COUNT(*) `COUNT` FROM member WHERE userid=#{userid}
    </select>


    <!-- 데이터 저장을 위한 기능 정의 -->
    <insert id="insertItem">
        INSERT INTO member (username, userid, userpw, useraddress, userphone, useremail, gender, birthdate, secession, signuptime) VALUES (#{username}, #{userid}, #{userpw}, #{useraddress}, ${userphone}, #{useremail}, #{gender}, #{birthdate}, 'T', now())
    </insert>

    <!-- 데이터 삭제를 위한 기능 정의 -->
    <delete id="deleteItem">
        DELETE FROM department WHERE deptno=#{deptno}
    </delete>

    <!-- 데이터 갱신을 위한 기능 정의 -->
    <update id="updateItem">
        UPDATE department SET dname=#{dname}, loc=#{loc}
        WHERE deptno=#{deptno}
    </update>

    <!-- 데이터 수 조회하기  -->
    <select id="selectCountAll">
        SELECT COUNT(*) AS cnt FROM department

        <where>
            <if test="dname != null and dname != ''">
                dname LIKE concat('%', #{dname}, '%')
            </if>

            <if test="loc != null and loc != ''">
                OR loc LIKE concat('%', #{loc}, '%')
            </if>
        </where>
    </select>

</mapper>
